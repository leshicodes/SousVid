fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# yt-dlp is intentionally unpinned: platform extractors (Instagram, TikTok, YouTube)
# break frequently and require up-to-date versions to keep working. Update manually
# when a new version is available -- treat it like a security patch for this project.
yt-dlp

faster-whisper==1.1.1

# openai SDK pointed at OpenRouter for LLM calls
openai==1.59.6

# httpx is the HTTP client for Mealie API calls; also a transitive dep of openai
httpx==0.28.1

# requests is required by faster-whisper internally (HuggingFace model downloads).
# Our own code uses httpx, but this must stay.
requests==2.32.3

python-multipart==0.0.20

# Job queue: Celery with Redis broker/backend
celery[redis]==5.4.0
redis==5.2.1

# Image processing for recipe photos
Pillow>=10.0.0

# Auth: JWT tokens and password hashing
# Using bcrypt directly (not via passlib) for Python 3.13 + bcrypt >= 4.x compatibility.
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
